# CLI版 セットアップ手順

`cli/index.ts` で提供される、認定調査票自動作成ツールのセットアップ手順です。データ（セッション・エクスポート結果）はローカルマシンのみに保存されますが、AI判定・確認チャット・特記事項チェックの際はGemini APIへの通信が発生します。
（ブラウザ版のセットアップは [README.md](README.md) を参照してください）

## ステップ 1: Gemini APIキーを取得

1. [Google AI Studio](https://aistudio.google.com/apikey) にアクセス
2. アカウントを作成（未作成の場合）
3. 「Get API key」からキーを生成
4. 生成されたキーをコピー

### Google Cloud プロジェクトの課金設定について

「学習・人間レビューに使われない」という保証を実際に受けるには、`GEMINI_API_KEY`が紐づくGoogle Cloudプロジェクトで**課金（有料枠）が有効になっている必要があります**。無料枠やnonprofitプログラムの利用枠だけでは、この保証は前提になりません。要配慮個人情報（利用者の障害・健康状態等）を扱う前に、Google Cloud Consoleの「お支払い」画面で課金が有効になっているか必ず確認してください。

## ステップ 2: 環境設定

`.env.example` を `.env` にコピーします。

```bash
cp .env.example .env
```

`.env` を開き、`GEMINI_API_KEY` にコピーしたキーを貼り付けます。

```
GEMINI_API_KEY=xxxxxxxxxxxxx   ← ここを実際のキーに書き換える
```

`GEMINI_API_KEY`はCLI版・ブラウザ版の両方で必要です。

## ステップ 3: 依存関係をインストール

```bash
npm install
```

## ステップ 4: CLIを起動

```bash
npm run cli
```

`.env` が見つからない場合や `GEMINI_API_KEY` が未設定の場合はエラーで終了します。

## 使用フロー

CLI起動後、メインメニューから操作します。

1. **新しい認定調査を開始** — 利用者名・年齢（任意）・備考（任意）を入力
2. **聞き取り内容の入力** — 調査で聞き取った内容を貼り付け、空行で入力終了
3. **AI確認チャット＆自動判定** — Gemini APIで80項目を自動判定。情報が不足している項目についてはAIが確認の質問をし、回答を踏まえて再判定する（実行中は1-2分程度）
4. **特記事項の編集** — 判定結果を確認しながら項目ごとに特記事項を入力。任意でAIによる特記事項チェック（選択した判定水準と記述内容が整合しているかの助言のみ、文章の書き換えは行わない）を実行
5. **結果のエクスポート** — JSON または CSV 形式で `.nintei/exports/` に出力

「**前回のセッションを再開**」「**セッション一覧を表示**」から、保存済みセッションの続きや一覧確認も可能です。

## データセキュリティ

- 📍 **データ保存はローカルのみ**: セッション・エクスポート結果はマシン上のファイルのみに保存
- 📡 **AI判定・確認チャット・特記事項チェックの際はGemini APIへ通信が発生します**（詳細は「Google Cloud プロジェクトの課金設定について」を参照）
- 🔐 **APIキーは`.env`でのみ管理**: リポジトリには含まれず（`.gitignore`済み）、`.env.example`のみが共有されます
- 🗑️ **削除可能**: セッションは `.nintei/sessions/` に、エクスポート結果は `.nintei/exports/` に保存され、フォルダごといつでも削除可能

## ファイル構成

```
.
├── .env                       ← APIキー等（gitignore対象、各自で作成）
├── .env.example                ← 設定項目のテンプレート
├── cli/
│   ├── index.ts                 ← CLIエントリーポイント（メインメニュー・入力フロー）
│   ├── assessmentEngine.ts      ← Gemini APIによる項目判定ロジック
│   ├── assessmentItems.ts       ← 80項目のデータ定義
│   ├── sessionManager.ts        ← セッションの保存・読込・エクスポート処理
│   └── engine/
│       ├── shared.ts             ← Gemini呼び出しの共通処理
│       ├── chatEngine.ts         ← AI確認チャット（情報不足項目への質問応答）
│       └── reviewEngine.ts       ← 特記事項アドバイザー（助言のみ、書き換えなし）
└── .nintei/                    ← 実行時に自動生成されるデータディレクトリ
    ├── sessions/                ← セッションごとのJSONファイル
    └── exports/                 ← JSON/CSVエクスポート結果
```

## トラブルシューティング

**問題: `GEMINI_API_KEY環境変数が設定されていません。`**
解決: `.env` ファイルが存在し、`GEMINI_API_KEY=...` の形式で正しく設定されているか確認してください。

**問題: Gemini API エラー / `Gemini応答のJSON解析に失敗しました`**
解決: APIキーが有効か、[Google AI Studio](https://aistudio.google.com/apikey) でアカウントの利用枠・残高が残っているか確認してください。

---

インストール完了後、`npm run cli` で起動できます。🎉
