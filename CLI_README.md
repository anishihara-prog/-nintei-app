# 認定調査票 自動作成ツール（CLI版）

**データ保存はローカルで完結（AI判定にはGemini APIへの通信が発生します）**

ブラウザを使わずに、ローカルマシンのみで認定調査票を自動作成するCLIツールです。

## 特徴

- ✅ **データはローカルマシンのみに保存**: セッション・エクスポート結果はマシン上のファイルのみに保存（AI判定・確認チャットの処理内容はGemini APIへの通信が発生します）
- ✅ **AI確認チャット＋自動判定**: Google Gemini APIを使用し、情報が不足している項目についてはAIが確認の質問をした上で80項目を自動判定
- ✅ **セキュア**: APIキーはローカル設定ファイル（`.env`）に保存
- ✅ **シンプル**: 聞き取り内容を入力するだけで調査票が完成
- ✅ **カスタマイズ可能**: 特記事項は相談員が編集可能。AIは書き換えを行わず、選択した判定水準と記述内容が整合しているかを助言するのみ

## セットアップ

### 1. Gemini API キーの取得

1. [Google AI Studio](https://aistudio.google.com/apikey) にアクセス
2. アカウントを作成 / ログイン
3. API キーを生成して、コピー

### Google Cloud プロジェクトの課金設定について

「学習・人間レビューに使われない」という保証を実際に受けるには、`GEMINI_API_KEY`が紐づくGoogle Cloudプロジェクトで**課金（有料枠）が有効になっている必要があります**。無料枠やnonprofitプログラムの利用枠だけでは、この保証は前提になりません。要配慮個人情報（利用者の障害・健康状態等）を扱う前に、Google Cloud Consoleの「お支払い」画面で課金が有効になっているか必ず確認してください。

### 2. 環境設定

```bash
# .env.example をコピー
cp .env.example .env

# .env ファイルを編集して API キーを設定
# GEMINI_API_KEY=xxxxxxxxxxxxx
```

### 3. 依存関係のインストール

```bash
npm install
```

## 使い方

### CLIを起動

```bash
npm run cli
```

### 操作フロー

```
1. メニューから「新しい認定調査を開始」を選択
2. 利用者名と年齢を入力
3. 調査の際に聞き取った内容を入力（複数行可能、空行で完了）
4. AI（Gemini）との確認チャット → 80項目を自動判定（1-2分）
5. 判定結果を確認し、項目ごとに特記事項を編集。任意でAIによる特記事項チェック（書き換えなし、助言のみ）を実行
6. 結果をエクスポート（JSON/CSV）
```

## ファイル構成

```
.nintei/
├── sessions/          # 保存されたセッションデータ
│   └── session-*.json
└── exports/           # エクスポートされたファイル
    ├── *.json
    └── *.csv
```

## トラブルシューティング

### エラー: `GEMINI_API_KEY環境変数が設定されていません。`

→ `.env` ファイルが存在し、`GEMINI_API_KEY` が正しく設定されているか確認してください

### エラー: `Gemini応答のJSON解析に失敗しました`

→ APIキーが有効か、または回数制限に達していないか確認してください

### セッションが保存されない

→ `.nintei/` ディレクトリに書き込み権限があるか確認してください

## セッションデータの構造

各セッションは以下の情報を含みます：

```json
{
  "id": "session-1707xxx",
  "timestamp": "2024-01-01T12:00:00Z",
  "clientInfo": {
    "name": "田中太郎",
    "age": 75,
    "notes": "糖尿病あり"
  },
  "intakeText": "聞き取り内容...",
  "judgements": {
    "1-1": "1.支援不要",
    "1-2": "2.見守り等",
    ...
  },
  "specialNotes": {
    "summary": "特記事項..."
  },
  "status": "completed"
}
```

## プライバシーについて

- 📍 **データ保存はローカルのみ**: セッション・エクスポート結果はあなたのマシンのみに保存されます（ブラウザ版と異なり画面共有等の経路がありません）
- 📡 **AI判定・確認チャット・特記事項チェックの際はGemini APIへ通信が発生します**: 「学習・人間レビューに使われない」保証を受けるには、課金（有料枠）を有効にしたGoogle Cloudプロジェクトのキーを使用してください（詳細は本ファイル冒頭「Google Cloud プロジェクトの課金設定について」を参照）
- 🔐 **APIキーの保護**: 環境変数として安全に管理
- 🗑️ **削除可能**: セッションはいつでも削除可能

## ライセンス

このプロジェクトはプライベートプロジェクトです。

## サポート

問題が発生した場合は、GitHub Issue を作成してください。
