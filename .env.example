# Gemini API設定（CLI・ウェブUI共通）
# 注意: 「学習に利用しない」保証を受けるには、このキーが紐づくGoogle Cloud
# プロジェクトで課金（有料枠）が有効になっている必要があります。
# 無料枠やnonprofitプログラムの利用枠だけでは保証されません。
# Google Cloud Console の「お支払い」画面で必ず確認してください。
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# ポート
PORT=8787

# CLI設定
DATA_DIR=.nintei
DEBUG=false
